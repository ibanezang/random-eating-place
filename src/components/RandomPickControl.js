import React from 'react';

function RandomPickControl(props){
    return (
        <div>
            <button className="btn btn-primary">Pick Random</button>
            {}            
        </div>
    );
}

export default RandomPickControl;